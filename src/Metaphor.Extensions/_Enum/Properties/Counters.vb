Friend NotInheritable Class Counters
    Private Sub New() : End Sub
    Friend Const ATTACK As String = NameOf(ATTACK)
    Friend Const ATTACK_CAP As String = NameOf(ATTACK_CAP)
    Friend Const DEFEND As String = NameOf(DEFEND)
    Friend Const DEFEND_CAP As String = NameOf(DEFEND_CAP)
    Friend Const DELTA_X As String = NameOf(DELTA_X)
    Friend Const DELTA_Y As String = NameOf(DELTA_Y)
    Friend Const EQUIP_FLAGS As String = NameOf(EQUIP_FLAGS)
    Friend Const HEALTH As String = NameOf(HEALTH)
End Class
